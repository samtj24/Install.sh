# Install.sh
# 🐧 Полный гайд по установке Arch Linux и настройке i3wm

> **Дружелюбное руководство для новичков**

## 📋 Что мы будем делать?

Этот гайд поможет вам установить Arch Linux с помощью готового скрипта и настроить красивый, минималистичный рабочий стол i3wm.

**Что делает наш скрипт установки:**
- 🖥️ **BIOS разметка диска** (для старых компьютеров без UEFI)
- 💾 **Swap раздел 2 ГБ** (виртуальная память - как продолжение оперативной памяти когда её не хватает)
- 📁 **Корневой раздел ext4** (основная файловая система Linux)
- 👤 **Создание пользователя** с правами администратора (sudo)
- 🌐 **Настройка сети** через NetworkManager (удобное управление Wi-Fi и интернетом)
- 🚀 **Установка GRUB** загрузчика (программа, которая запускает систему)
- 📦 **Базовые пакеты** включая браузеры, офис и системные утилиты

---

## ⚠️ ВАЖНЫЕ ПРЕДУПРЕЖДЕНИЯ

### 🔥 Опасность потери данных!
**ВСЕ ДАННЫЕ НА ВЫБРАННОМ ДИСКЕ БУДУТ ПОЛНОСТЬЮ УДАЛЕНЫ!**

- Сделайте резервные копии важных файлов
- Убедитесь, что выбираете правильный диск
- Проверьте диски командой `lsblk` несколько раз

### 🖥️ Проверьте тип вашей системы
Этот гайд предназначен для **BIOS (Legacy) систем**. Если у вас современный компьютер с UEFI, см. раздел ["Что если у вас UEFI?"](#что-если-у-вас-uefi)

---

## 🚀 Часть 1: Установка Arch Linux

### Шаг 1: Подготовка к установке

**Перед запуском скрипта обязательно:**

1. **Загрузитесь с установочного ISO Arch Linux**
2. **Подключитесь к интернету:**
   ```bash
   # Для Wi-Fi используйте iwctl:
   iwctl
   station wlan0 scan
   station wlan0 get-networks
   station wlan0 connect "Имя_сети"
   exit
   
   # Проверьте подключение:
   ping google.com
   ```
3. **Проверьте доступные диски:**
   ```bash
   lsblk
   # Пример вывода:
   # NAME   MAJ:MIN RM  SIZE RO TYPE MOUNTPOINT
   # sda      8:0    0  500G  0 disk 
   # nvme0n1  8:0    0  256G  0 disk
   ```

### Шаг 2: Запуск скрипта установки

```bash
# Сделайте скрипт исполняемым
chmod +x install_arch.sh

# Запустите установку
./install_arch.sh
```

### Шаг 3: Подробный разбор работы скрипта

#### 🔍 1. Ввод данных пользователя

**Что спросит скрипт:**
- **Диск для установки**: `/dev/sda` (обычные диски) или `/dev/nvme0n1` (SSD NVMe)
- **Имя компьютера**: любое английское имя без пробелов
- **Имя пользователя**: ваш логин (только латиница, без пробелов)
- **Пароли**: для пользователя и администратора (root)

**🔒 О безопасности паролей:**
Скрипт передает пароли через переменные для удобства автоматизации. В реальной работе рекомендуется устанавливать пароли вручную после установки системы.

#### 🛡️ 2. Проверки безопасности

Скрипт автоматически проверяет:
- **Существование диска** - защита от опечаток
- **Минимальный размер 8 ГБ** - гарантия, что система поместится
- **Подтверждение пользователя** - последний шанс передумать!

#### 💾 3. Разметка диска (что происходит с вашим диском)

```bash
# Создается старая MBR таблица разделов (для BIOS)
parted "$DISK" --script mklabel msdos \
 mkpart primary linux-swap 1MiB 2GiB \      # Swap - виртуальная память
 mkpart primary ext4 2GiB 100% \            # Корневой раздел - основная система  
 set 2 boot on                              # Делаем раздел загрузочным
```

**Простыми словами:**
- Первые 2 ГБ = виртуальная память (когда не хватает оперативной)
- Остальное место = ваша операционная система и файлы

#### 📦 4. Установка пакетов

**Что устанавливается и зачем:**

**Базовые пакеты:**
- `base` - основа системы (ядро команд)
- `base-devel` - инструменты для сборки программ из исходников
- `linux` - ядро операционной системы
- `linux-firmware` - драйверы для железа

**Редакторы:**
- `vim` - мощный текстовый редактор для профи
- `nano` - простой редактор для новичков

**Сеть и система:**
- `networkmanager` - простое управление интернетом
- `grub` - загрузчик системы
- `sudo` - выполнение команд от имени администратора

**Дополнительные программы:**
- `firefox`, `chromium` - браузеры
- `libreoffice-fresh` - офисный пакет (аналог Microsoft Office)
- `git` - система контроля версий
- `htop`, `tree` - удобные системные утилиты

#### ⚙️ 5. Настройка системы

**Что настраивается автоматически:**
- Часовой пояс (Asia/Dushanbe)
- Языки интерфейса (английский + русский)
- Пользователь с правами sudo
- Автозапуск сетевого менеджера
- Установка загрузчика GRUB

#### ✅ 6. Проверка после установки

После завершения скрипта можно дополнительно проверить:

```bash
# Войти в установленную систему для проверки
arch-chroot /mnt

# Проверить пользователей
cat /etc/passwd | grep ваш_логин

# Проверить sudo права
groups ваш_логин

# Выйти из chroot
exit
```

### 🚨 Возможные ошибки и решения

#### 1. "Диск не найден"
**Причина:** Неправильно указано имя диска
**Решение:** 
```bash
lsblk  # Проверьте точное имя диска
# Для SATA: /dev/sda, /dev/sdb
# Для NVMe: /dev/nvme0n1, /dev/nvme1n1
```

#### 2. "Диск слишком мал"
**Причина:** На диске меньше 8 ГБ места
**Решение:** Освободите место или используйте другой диск

#### 3. "Ошибка при форматировании"
**Причина:** Диск используется другой программой
**Решение:**
```bash
# Отмонтируйте все разделы диска
umount /dev/sda*
# Перезапустите скрипт
```

#### 4. "Нет интернета"
**Причина:** Отсутствует подключение к сети
**Решение:**
```bash
# Проверьте подключение
ping -c 3 8.8.8.8
# Переподключитесь к Wi-Fi через iwctl
```

---

## 🎨 Часть 2: Настройка красивого рабочего стола i3wm

### 🎯 Что такое i3wm?

i3wm - это "тайловый" оконный менеджер. Представьте, что ваши окна автоматически расставляются по экрану как плитки мозаики - никаких наложений, все программы видны одновременно. Это очень удобно для работы и потребляет мало ресурсов.

### Шаг 1: Первый вход в систему

После перезагрузки:
```bash
# 1. Войдите под созданным пользователем
# 2
